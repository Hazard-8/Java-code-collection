/*
    * final variable ,if you don't want others (or yourself) to overwrite existing values,
        use the final keyword (this will declare the variable as "final" or "constant",
                       which means unchangeable and read-only):
        e.g:- final int myNum = 15;
        myNum = 20;  // will generate an error: cannot assign a value to a final variable

    * System.exit(0); // This will end further execution of the program

    * For outputting "" in between output, Enter \ before "
        e.g:- ("Are you a Student: Enter \"(Y/N)\" OR \"(true / false).\"");

    *

    *

    *

*/